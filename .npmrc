# Hoist Tailwind native bindings so @tailwindcss/oxide can find them
public-hoist-pattern[]=@tailwindcss/oxide*
public-hoist-pattern[]=@tailwindcss/*
# Ensure optional dependencies are installed
optional=true
